export type EventProp = {
    id: number,
    title: string,
    description: string,
    date: string,
    asset: string
}

export type IssueProp = {
    id: number,
    name: string,
    content: string,
    asset: string,
    fullAsset: string
}
